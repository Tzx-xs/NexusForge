{
  "name": "nexus-local",
  "version": "0.1.0",
  "build": {
    "commands": [
      "npm ci",
      "npm run build"
    ],
    "output": "dist"
  },
  "routes": [
    {
      "pattern": "/*",
      "rewrite": "/index.html"
    }
  ]
}
